<p id="notice"><%= notice %></p>

<h1>Listing Nodes</h1>

<table class="table">
  <thead>
    <tr>
      <th>Date</th>
      <th>Count</th>
      <th colspan="2"></th>
    </tr>
  </thead>

  <tbody>
    <% @statistics_per_week.each do |date,count| %>
    <tr>
        <td><%= date %></td>
        <td><%= count %></td>
    </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Back', nodes_path %>

